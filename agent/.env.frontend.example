GITHUB_TOKEN=ghp_your_token_here
WEBHOOK_SECRET=your_webhook_secret_here
AGENT_LABEL=frontend
# gh CLI 完整路径，默认 "gh"；若 which gh 返回非标准路径则填入
# GH_PATH=/Users/yourname/bin/gh
# cloudflared 完整路径，默认 "cloudflared"；若 which cloudflared 找不到则填入
# CLOUDFLARED_PATH=/Users/yourname/bin/cloudflared

# ===== 事件驱动激活方式（Codex AI Agent） =====
# webhook 事件 → 写 ~/finance-system-tasks/issue-N.md + append 路径到 inbox.txt
# 你需要在另一个终端窗口运行 ./codex_listener.sh，监听 inbox 并喂给 codex
#
# TASKS_DIR=/Users/yourname/Desktop/tasks
# INBOX_FILE=...
# DESKTOP_NOTIFY=1
# AGENT_DISPATCH_CMD=（留空使用 inbox 模式）

# ===== UTF-8 兜底 =====
LANG=en_US.UTF-8
LC_ALL=en_US.UTF-8
PYTHONUTF8=1
PYTHONIOENCODING=utf-8
